/**
 * Video Model
 *
 * very simple model as it will be just used as data container
 *
 * @author Javier Mellado <sol@javiermellado.com>
 */

define(
    ['backbone'],
    function (Backbone){
        return Backbone.Model.extend({});
    }
);